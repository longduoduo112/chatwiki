interface:
  display_name: "Web2Skill"
  short_description: "Build an indexed skill from one site or a URL batch"
  default_prompt: "Use $Web2Skill to prepare URLs, crawl rendered pages, and package a bounded retrieval skill."
